package com.hypergraph.map_;

public class HashSetSource {
    /*
        一、k-v存放在HashMap$Node中
        tab[i] = newNode(hash, key, value, null);
        二、为方便遍历，还创建了 entrySet 集合，集合中存放的数据类型是 Map$Entry
        三、运行时，entrySet里实际存放的还是HashMap$Node
     */
}
